import 'package:flutter/material.dart';


class StartScrren extends StatelessWidget {
  const StartScrren({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        mainAxisSize: MainAxisSize.min,
        children: [

        ],
      ),
    );
  }
}
